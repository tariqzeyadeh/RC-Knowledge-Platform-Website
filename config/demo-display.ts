/**
 * Toggle static / seeded demo content in page UIs.
 * Set to `true` to restore fixture-backed and seeded catalog displays.
 * Fixture files under `data/fixtures/` are never deleted — used by seed scripts.
 */
export const RENDER_STATIC_DEMO_DATA = false
