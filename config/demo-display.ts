/**
 * Toggle static / seeded demo catalog content in page UIs.
 * Set to `true` to restore fixture-backed catalog displays (analytics, features, home KPIs, etc.).
 * Live workflow data (review queue, uploads, CRUD lists, home recent assets/categories) is always shown regardless of this flag.
 */
export const RENDER_STATIC_DEMO_DATA = false
